## 1. README

- [x] 1.1 Add a "Live app: https://estebanmdq.github.io/fretboard-master/" line to `README.md`, after the description paragraph and before the Development section

## 2. App header link

- [x] 2.1 Add a `<a>` link to `https://github.com/EstebanMDQ/fretboard-master` in the `app-shell__header` element in `AppShell.tsx`, with `target="_blank" rel="noopener noreferrer"` and visible text "GitHub"
- [x] 2.2 Add CSS to `AppShell.css` to position the link to the right of the title (flex layout on the header)

## 3. Verify

- [x] 3.1 Run `npm run build` and confirm no TypeScript or build errors
- [x] 3.2 Open the dev server and confirm the GitHub link appears in the header and opens the correct URL
